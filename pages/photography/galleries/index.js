import PhotoLayout, {
    siteTitle,
} from '../../../components/layouts/PhotoLayout/PhotoLayout';

// index page is going to be the splash page
const Home = () => {
    return (
        <PhotoLayout>
            <h2>Galleries</h2>
        </PhotoLayout>
    );
};
export default Home;
