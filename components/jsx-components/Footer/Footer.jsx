import styles from './Footer.module.scss';

const Footer = () => {
    return (
        <footer>
            <p className={styles.text}>Clay Kaufmann</p>
        </footer>
    );
};
export default Footer;
